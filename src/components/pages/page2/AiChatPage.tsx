const AiChatpage = () => {
  return (
    <div className="placeholder-panel">
      <span className="placeholder-chip">AI workspace</span>
      <h2>AI chat UI is prepared</h2>
      <p>This page now has a friendly placeholder so the screen feels intentional until the AI conversation logic is added.</p>
    </div>
  );
};

export default AiChatpage;
